import { spacing } from "../../styles/spacing";

export const styles = {
  section: {
    maxWidth: "1280px",
    margin: `${spacing.xxxl} auto`,
    padding: `0 ${spacing.base}`,
  },

  grid: {
    width: "100%",
  },
};
